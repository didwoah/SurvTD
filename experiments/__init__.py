# Package marker. Without this file `unittest discover` reports
# "Start directory is not importable" and the suite never runs as a suite --
# which is how it went unnoticed that these tests were not being executed.
